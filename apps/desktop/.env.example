# Lobby WebSocket server URL (default: ws://localhost:8080)
VITE_WS_URL=ws://localhost:8080

# Local emulator launch API base URL (default: http://localhost:8080)
# Set this if the lobby server is running on a non-default port.
VITE_LAUNCH_API_URL=http://localhost:8080
